"use client";

// TODO: Replaced in Task 24 with full implementation (markdown rendering,
// /api/notes/by-path lookup, bin chips, Esc to close, external links).
// This stub exists only so Task 22 builds clean.

export function ReadingPane(_props: { path: string; onClose: () => void }) {
  return null;
}
